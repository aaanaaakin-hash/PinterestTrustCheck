# Движок Go и рабочие папки — скачиваются сами через install.bat
.tools/
# Собранная программа — лежит в архиве релиза
bin/
release/
# Личное: ключи, история, итоги — никогда не попадают в репозиторий
gsb.key
otx.key
vt.key
history.csv
results.csv

archive/
.repowise/
