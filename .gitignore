# Движок Go и рабочие папки — скачиваются сами через install.bat
.tools/
# Собранная программа — лежит в архиве релиза
bin/
release/
# Личное: ключ, история, итоги — никогда не попадают в репозиторий
gsb.key
history.csv
results.csv
